<Window x:Class="Microsoft.Samples.Kinect.DepthBasics.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="HandMap" 
        Height="600" Width="1046.854" 
        WindowState="Maximized"
        WindowStyle="None"
        Background="Transparent"
        AllowsTransparency="True"
        Closing="MainWindow_Closing"> 
  <Window.Resources>
    <SolidColorBrush x:Key="MediumGreyBrush" Color="#ff6e6e6e" />
    <SolidColorBrush x:Key="KinectPurpleBrush" Color="#ff52318f" />
    <SolidColorBrush x:Key="KinectBlueBrush" Color="#ff00BCF2" />
    <Style TargetType="{x:Type Image}">
      <Setter Property="SnapsToDevicePixels" Value="True" />
    </Style>
   
  </Window.Resources>
    <Grid Margin="10 0 10 0">
    <Grid.RowDefinitions>
      <RowDefinition Height="Auto" />
      <RowDefinition Height="*" />
      <RowDefinition Height="Auto" />
      <RowDefinition Height="Auto" />
    </Grid.RowDefinitions>

        <Grid Grid.Row="0" Background="White">
            <Image  Source="Images\Logo.png" HorizontalAlignment="Left" Stretch="Fill" Height="32" Width="81" Margin="275,5,0,10" />
            <TextBlock Margin="0,0,108,13" HorizontalAlignment="Right" VerticalAlignment="Bottom" Foreground="{StaticResource KinectPurpleBrush}" FontFamily="Segoe UI" FontSize="18" RenderTransformOrigin="-0.098,0.507">HandMap</TextBlock>
            <Button Width="47" Height="47" HorizontalAlignment="Right" RenderTransformOrigin="20.928,0.166" Click="Button_Close">x</Button>
        </Grid>
    <Grid Grid.Row="1" HorizontalAlignment="Center">
            <Image Source="{Binding DepthSource}" Stretch="UniformToFill" />            
            <Image Source="{Binding JointSource}" Stretch="UniformToFill"/>
     </Grid>


        <!--<Viewbox Grid.Row="2" HorizontalAlignment="Center">
            <Grid>
                <Image Source="{Binding ImageSource}" Stretch="UniformToFill"/>                
            </Grid>
        </Viewbox>-->


        <StatusBar Grid.Row="3" HorizontalAlignment="Stretch" Name="statusBar" VerticalAlignment="Bottom" Background="White" Foreground="{StaticResource MediumGreyBrush}">
      <StatusBarItem Content="{Binding StatusText}" />
    </StatusBar>
  </Grid>
</Window>
